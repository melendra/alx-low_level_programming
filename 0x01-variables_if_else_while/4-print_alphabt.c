#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main - Entry point
 *Desscription: 'Program that prints the alphabet in lowercase'
 *Reurn: Always 0 (Success)
 */

int main(void)
{
char(x) = 'a';

while (x <= 'z')
{
	{
	if (x != 'q' && x != 'e')
		putchar(x);
	}
	x++;

}

putchar('\n');

return (0);
}
