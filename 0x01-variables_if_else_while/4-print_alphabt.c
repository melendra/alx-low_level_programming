#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main(void) {
	char(x) = 'a';

	while (x <= 'z') {

	if (x != 'q' && x != 'e') {
		putchar(x);
return (0);
	}
	x++;

	}

	putchar('\n');

	return (0);
}
