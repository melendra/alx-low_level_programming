#include <stdio.h>
/**
 *main - Entry point
 *Description: 'Print alphabets lowercase'
 *Return: Always 0
 */
int main(void)
{
	char(x) = 'a';

	while (x <= 'z')
	{

	if (x != 'q' && x != 'e')
	{
		putchar(x);
return (0);
	}
	x++;

	}

	putchar('\n');

	return (0);
}
